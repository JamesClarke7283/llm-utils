[package]
name = "llm-codebase-to-prompt"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
clap = {version="4", features = ["derive"]}
glob = "0.3"
notify = "6.1"
ignore = "0.4"

